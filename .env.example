# Example environment variables (copy to .env)
# This file is safe to commit.

FLASK_ENV=development
SECRET_KEY=change-me

# SQLite (dev)
DATABASE_URL=sqlite:///portfolio.db

# Allow React dev server
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Optional: SMTP settings for contact email delivery
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_USE_TLS=true
CONTACT_TO_EMAIL=
CONTACT_FROM_EMAIL=

# --- Free deployment (Cloudflare Workers + Supabase) ---
# These are NOT used by the Flask backend; they're used by the Worker in /worker.
# Store the service role key as a secret in Cloudflare (do not commit it anywhere).
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
